# ── Runtime output (scan results, session data, logs) ───────────────────────
# JsFuzzer session data (real target JS / findings)
output/
# Saudit scan results and logs (if saudit is run from this directory)
results/
logs/
# Ansible/deps installer cache
cache/
# Saudit presets / installed binaries (if saudit is run from this directory)
presets/
tools/

# ── Python / macOS ───────────────────────────────────────────────────────────
.DS_Store
__pycache__/
*.pyc
.venv/
